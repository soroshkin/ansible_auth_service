- name: debugger
  debug: 
    msg: "{{ hostvars['postgres'].ansible_host }}"

#    msg: "{{ groups['postgres'] }}"
  #with_items: "{{ groups['postgres'] }}"


- name: de
  debug:
    msg: "{{ postgres_password }}"


- name: debug
  debug: 
    msg: "{{ groups['authservice_api']  | map ('extract', hostvars, ['inventory_hostname']) | join(', ')}}"
